'use strict';

describe('myApp.quiz module', function() {

  beforeEach(module('myApp.quiz'));

  describe('quiz controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var quizCtrl = $controller('Quiz1Ctrl');
      expect(quiz1Ctrl).toBeDefined();
    }));

  });
});